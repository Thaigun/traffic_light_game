package mapLogic

import java.io._
import scala.io._

class FileReader {
  var file: File = null
  var fileAsText = ""
  
  def setFile(newFile: File): Unit = {
    file = newFile
    setFile()
  }
  
  def setFile() = {
    if (file == null) throw new Exception("The game file must be defined first!")
  }
  
  def getNumberOfCars() = {
    
  }
}